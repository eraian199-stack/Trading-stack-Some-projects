# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
build/
dist/

# OS
.DS_Store

# Local IDE / Claude config (machine-specific: absolute paths, local settings)
.claude/

# Virtualenvs
.venv/
venv/

# Generated / downloaded (regenerable, can be large; not part of the stack)
data/cache/
outputs/
# Live results ledger: accumulates on disk as games are played (per-deployment
# state, rebuilt from the feeds on a fresh checkout). Not a committed artifact.
data/world_cup_2026_results.csv
# Squad-value table: derived from the external CC0 Transfermarkt dataset,
# rebuilt on demand by data/squad_value.py. Not a committed artifact.
data/squad_value_by_year.csv
# User/exported ability overlays (e.g. SofaScore ratings) for the squad overlay;
# per-deployment snapshots, refreshed by re-export. Not committed.
data/ability_overlay_*.csv

# Original uploaded reference framework (inputs that were merged into the stack)
docs/files/
docs/files.zip
